#intro<
  Good
  - if Time.now.hour < 12
    morning,
  - elsif Time.now.hour > 18
    evening,
  - else
    afternoon,
  = current_user.name.capitalize
  \.
  %br/
  %span#button-intro
    = link_to "Create Your Day", new_day_path
